# AI Software Engineer - Environment Variables
# Usage: copy (or rename) this file to `.env` and adjust values as needed.
# Notes:
#   - Do not commit your real `.env` if it contains secrets.
#   - Keep values without quotes unless explicitly shown.
#   - Comment lines starting with `#` are ignored by dotenv parsers.

# Project name
COMPOSE_PROJECT_NAME=ai-software-engineer

# Registry (e.g. docker.io, ghcr.io)
REGISTRY=docker.io

# Image
IMAGE_NAME=liukunup/ai-software-engineer
IMAGE_TAG=latest

# Comma-separated allow‑list of shell commands the worker may execute.
# SECURITY: Keep this list minimal; each command runs with container user privileges.
ALLOWED_COMMANDS=echo,date,ls
# Default per-command execution timeout (seconds)
COMMAND_TIMEOUT=15

# Node operating mode
# - worker (connects to backend)
# - standalone (self-contained)
NODE_MODE=worker
# Backend registration endpoint (required when NODE_MODE=worker)
REGISTER_URL=http://localhost:8000
REGISTER_KEY=key
# Node metadata
NODE_NAME=ai-software-engineer
NODE_HOST=
NODE_DESC=AI-Software-Engineer
NODE_TAGS=worker,ai
# Heartbeat interval (seconds) for worker -> backend status updates
HEARTBEAT_INTERVAL=30
